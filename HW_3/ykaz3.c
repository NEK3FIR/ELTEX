#include <stdio.h> 
 
int main() { 
    // Определяем массив из 10 элементов и заполняем его числами от 1 до 10 
    int array[10]; 
    for (int i = 0; i < 10; i++) { 
        array[i] = i + 1; 
    } 
 
    // Указатель на первый элемент массива 
    int *ptr = array; 
 
    // Вывод элементов массива с использованием указателя и арифметики указателей 
    for (int i = 0; i < 10; i++) { 
        printf("%d ", *(ptr + i)); 
    } 
    printf("\n"); 
 
    return 0; 
}
